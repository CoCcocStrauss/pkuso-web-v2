# 理解项目架构

> **Workspace:** c:\Users\dddam\Desktop\pkusoweb

---

## User
先阅读我的项目，理解我的项目的整体架构

## Assistant
我将帮你阅读并理解这个项目的整体架构。让我先查看项目的基本结构和关键文件。



让我重新定位项目目录：



现在让我阅读关键文件来理解项目架构：



让我使用正确的路径（注意是 `pkusoweb-v2` 而不是 `pkusoweb-v2`）：



现在让我查看核心组件和配置文件：



现在让我查看 hooks 和主要页面：



现在让我查看其他页面和 API 路由：



现在让我查看一些辅助文件和组件：



现在让我查看剩余的 hooks 和组件文件：



现在让我查看最后一个组件文件：



非常好！我已经详细阅读了整个项目的代码。现在让我为你总结这个项目的整体架构：

## 📊 项目架构总结

### 🎯 项目概述
这是一个**乐团管理助手**（Orchestra Management Assistant）Web 应用，基于 Next.js 16 + React 19 构建，面向乐团成员的移动端管理助手。

---

### 🏗️ 技术栈

**前端框架：**
- Next.js 16 (App Router)
- React 19
- TypeScript
- Tailwind CSS v4

**后端服务：**
- Supabase (BaaS - 数据库 + 认证 + 存储)
- Resend (邮件发送服务)

**核心库：**
- `@supabase/supabase-js`: 数据库客户端
- `react-datepicker`: 日期选择器
- `lucide-react`: 图标库
- `browser-image-compression`: 图片压缩
- `date-fns`: 日期处理

---

### 📁 项目结构

```
pkuso-web-v2/
├── src/
│   ├── app/                      # Next.js App Router 路由
│   │   ├── (auth)/               # 认证相关页面（登录/注册/忘记密码）
│   │   ├── api/                  # API 路由（邮件通知）
│   │   ├── community/            # 社区页面（重奏/团建公告）
│   │   ├── members/              # 成员页面（花名册/考勤统计）
│   │   ├── profile/              # 个人中心
│   │   ├── sections/             # 可复用页面组件
│   │   ├── layout.tsx            # 根布局
│   │   └── page.tsx              # 首页（日程 + 公告）
│   │
│   ├── components/               # UI 组件
│   │   ├── modal/                # 功能模态框
│   │   │   ├── PublishRehearsalModal.tsx    # 发布排练
│   │   │   └── AttendanceManageModal.tsx    # 考勤管理
│   │   └── ui/                   # 基础 UI 组件
│   │       ├── Modal.tsx
│   │       └── Toggle.tsx
│   │
│   ├── context/                  # React Context
│   │   └── UserContext.tsx       # 用户全局状态
│   │
│   ├── hooks/                    # 自定义 Hooks
│   │   ├── useAnnouncements.ts   # 公告管理
│   │   ├── useRehearsals.ts      # 排练日程管理
│   │   ├── useMembers.ts         # 成员管理
│   │   └── useAttendance.ts      # 考勤管理
│   │
│   └── lib/                      # 工具库
│       ├── constants.ts          # 常量定义
│       ├── enums.ts              # 枚举定义
│       ├── types.ts              # TypeScript 类型
│       ├── utils.ts              # 工具函数
│       ├── supabase.ts           # 客户端 Supabase
│       └── supabase-server.ts    # 服务端 Supabase
│
└── public/                       # 静态资源
```

---

### 🔑 核心功能模块

#### 1️⃣ **认证系统 (Auth)**
- **登录/注册/忘记密码**
- **邀请码机制**：注册需要邀请码（硬编码为 `PKUSO2026`）
- **审批流程**：用户注册后状态为 `pending`，需管理员审批为 `approved` 才能登录
- **权限控制**：通过 `AuthGate` 组件实现路由守卫
- **角色系统**：`admin`（管理员）和 `member`（普通成员）

#### 2️⃣ **首页 (日程 + 公告)**
- **公告模块**：显示最新的全团公告（管理员可发布）
- **排练日程模块**：
  - 显示合排/分排日程
  - 成员签到功能（合排需签到码，分排直接签到）
  - 管理员可编辑/删除日程、管理考勤

#### 3️⃣ **社区 (Community)**
- **公告板**：发布重奏（ensemble）和团建（gathering）信息
- **帖子功能**：
  - 支持图片上传（压缩后上传到 Supabase Storage）
  - 声部信息（已有声部/缺少声部）
  - 编辑/删除功能（作者或管理员可操作）
  - 联系方式一键复制

#### 4️⃣ **成员 (Members)**
- **排练考勤统计**：
  - 选择排练区间统计出勤次数
  - 按声部 - 姓名分组显示
  - 导出 CSV 功能
- **花名册**：
  - 按乐器分组显示成员信息
  - 显示学院、邮箱、入团时间等

#### 5️⃣ **个人中心 (Profile)**
- **个人信息展示**：姓名、声部、邮箱
- **修改密码**
- **管理员控制台**：
  - 入团审批（查看待审批用户并批准）
  - 发布全团公告
- **写评论**：提交反馈建议

---

### 🗄️ 数据库设计（Supabase）

**主要数据表：**

1. **`profiles`** - 用户资料
   - `id` (uuid, PK) - 关联 Supabase Auth
   - `full_name`, `email`, `instrument`, `college`, `join_date`
   - `status` (pending/approved) - 审批状态
   - `role` (admin/member) - 角色

2. **`announcements`** - 全团公告
   - `id`, `created_at`, `content`

3. **`rehearsals`** - 排练日程
   - `id`, `title`, `date`, `start_time`, `end_time`
   - `location`, `repertoire`, `type` (full/section)
   - `target_section`, `sign_in_code`

4. **`attendances`** - 考勤记录
   - `id`, `rehearsal_id`, `user_id`, `status`, `created_at`

5. **`posts`** - 社区帖子
   - `id`, `author_id`, `title`, `content`, `type`
   - `current_sections`, `missing_sections`, `contact_info`
   - `image_url`, `is_active`

6. **`comments`** - 评论/反馈
   - `title`, `content`

---

### 🔄 数据流

**客户端数据流：**
```
组件 → Custom Hook → Supabase Client → Supabase API → 数据库
         ↓
    状态管理 → 返回给组件
```

**认证流程：**
```
用户登录 → Supabase Auth → 获取 session
    ↓
AuthGate 检查 session → 查询 profiles 表
    ↓
根据 status/role 决定是否放行
```

**权限控制：**
- **管理员**：可发布/编辑/删除日程、公告，审批成员，管理考勤
- **普通成员**：只能查看、签到、发布社区帖子

---

### 🎨 UI/UX 特点

1. **移动端优先**：最大宽度限制为 `max-w-md`（448px），模拟 App 体验
2. **底部导航栏**：日程/社区/成员/我的 四个 Tab
3. **统一的卡片式设计**：圆角、阴影、浅色背景
4. **响应式反馈**：加载状态、错误提示、成功提示
5. **模态框交互**：底部弹出的 Modal 设计

---

### 🔐 安全特性

1. **RLS (Row Level Security)**：依赖 Supabase 的数据库级权限控制
2. **Service Role Key**：服务端 API 使用 service role 绕过 RLS
3. **邀请码验证**：注册时需要正确的邀请码
4. **审批机制**：新用户需管理员批准

---

### 📦 部署与配置

**环境变量：**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `RESEND_API_KEY`
- `ALLOWED_DEV_ORIGINS`

**开发服务器：**
- 运行在 `http://localhost:4000`
- 通过 `server.js` 自定义端口

---

### 🎯 项目亮点

1. ✅ **完整的权限系统**：基于角色的访问控制
2. ✅ **审批流程**：用户注册 → 待审批 → 批准
3. ✅ **考勤管理**：签到码机制 + 管理员补录
4. ✅ **邮件通知**：集成 Resend 发送邮件
5. ✅ **图片处理**：前端压缩后上传
6. ✅ **数据导出**：考勤统计导出 CSV
7. ✅ **响应式设计**：移动端优先的 UI

---

这是一个功能完整、架构清晰的乐团管理系统，涵盖了用户管理、日程管理、社区互动、考勤统计等核心场景，代码质量较高，使用了现代化的技术栈和最佳实践。